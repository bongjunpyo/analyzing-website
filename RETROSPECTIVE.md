# RETROSPECTIVE — analyzing-website

## 목표

공모전/경진대회 공고 URL을 받아 LLM으로 공고 정보를 구조화 추출하고, 2026 기술 트렌드
적합도와 아이디어를 분석해 옵시디언 러닝 로그에 누적하는 CLI. 하드코딩된 대회 URL 없이
어떤 공고 페이지에도 동작하는 범용 도구를 지향.

## 기술 선택 이유

- **Playwright(chromium) 렌더링**: 초기에는 정적 HTML만 받았으나(`a14185d`에서 Playwright 도입),
  공고 사이트가 JS로 본문을 렌더하는 경우가 많아 헤드리스 브라우저 렌더링으로 전환.
- **BeautifulSoup 본문 추출 + max_chars 제한**: LLM 토큰 절약. DOM 전체가 아니라 본문 텍스트만
  잘라 넣어 비용을 낮춤.
- **json_schema 구조화 출력 + 프롬프트 캐싱**: 자유 서술이 아니라 스키마 강제로 후처리
  안정성 확보, 시스템 프롬프트 ephemeral 캐싱으로 반복 호출 비용 절감 (`daae439`).
- **LLM 제공자 변경 이력**: `a14185d`에 "Update to Gemini API" 흔적이 있으나 현재 코드는
  Claude 기반. [확인 필요: Gemini→Claude 재전환 사유 — 스키마 강제/캐싱 때문인지]
- **표준 라이브러리만 쓰는 로그·리포트 모듈**: `obsidian_log.py`/`report.py`는 의존성 0.
  파싱·집계는 네트워크·API 없이 테스트 가능해야 한다는 원칙.

## 막힌 점과 해결

| 막힌 점 | 해결 |
|---|---|
| 러닝 로그 파서에 분석 텍스트가 마커(`<!-- src -->`)를 위조할 위험 | `_safe()`로 `<!--`/`-->` 무력화, 보안 테스트 추가 (`fb6faf6`) |
| 같은 공고를 다시 분석하면 로그에 중복 누적 | URL 마커 기준 **업서트**(섹션 교체) + 인덱스 표 재생성 |
| 수집한 공고가 쌓여도 전체를 조망할 통계가 없음 | 이번 작업에서 `report.py` 추가 — 분야/상금/D-day/트렌드 분포 집계 |
| 상금이 로그 메타에 구조화 저장되지 않음 | 요약문에 원문 금액 표현이 보존(프롬프트 규칙)됨을 이용해 정규식 추출(휴리스틱) |

## 다시 한다면

1. **상금을 처음부터 메타(JSON)에 정규화 저장**한다. 지금은 요약문 정규식에 의존해
   억/천만/만원 단위만 잡고, "USD"·"인원 T.O." 같은 표현은 놓친다.
2. LLM 제공자 추상화 레이어를 둔다 — Gemini↔Claude 전환에 코드 곳곳이 흔들렸다.
3. 리포트에 시계열(주차별 수집량)·중복 URL 감지 지표를 추가한다.
4. 파서/집계는 지금처럼 stdlib·순수 함수로 유지 — 네트워크 없는 테스트가 회귀를 빨리 잡았다.
